
## 全量回归测试（程小测模式，2026-08-17）
- 走查 10 页面 + 12 云函数 + utils/cloud|theme|icons|refresh + components/swipe-cell，产出 docs/defect-report-2026-08-17.md。
- **P0**：generateQRCode 云函数上传+return 整段被注释，分享二维码功能完全失效（需恢复 cloud.uploadFile+getTempFileURL+return）。
- **P1(6)**：① QR 保存缺 wx.downloadFile（直接 saveImageToPhotosAlbum 必失败）；② 上传图片未入库（t-upload 只给本地临时路径，未走 wx.cloud.uploadFile）；③ 真机 t-upload grid 添加按钮点击可能无响应（glass-easel 旧坑）；④ 通知提前天数/开关未持久化（mine 只 setData 不调云函数，getExpiringItems 永远读默认3天）；⑤ 物品详情编辑无法改保质期（onDatePicker 是 stub）。
- **P2(7)**：pages/logs 未注册死代码；fridge-create previewZones/rebuildPreview 未被 wxml 使用（单开门恒温层置中预览未生效）；fridge.ts 首屏 onLoad+onShow 双发请求；onLayerTap 直接 mutate this.data；item-detail 加载失败展示伪造数据可编辑；member-manage isOwner 判定为"列表有任意owner"(恒true)；t-image 显示 cloud:// 可能不渲染。
- 待确认：createFridge 默认冰箱修复需重新部署云函数验证；真机 t-upload/日期/iOS 行为需实机。

## 全量回归测试 第二轮（程小测模式，2026-08-17 下午）
- 验证第一轮 14 项修复：13/13 全部 PASS（含 createFridge 不抢占默认）。报告 docs/defect-report-2026-08-17-round2.md。
- 本轮新发现 20 个缺陷：**P0:1 / P1:2 / P2:17**。
  - **P0-01**：云函数日期时区错位——`new Date("2026-08-17")` 按 UTC 零点解析，+8 服务器上"今天到期"物品 08:00–23:59 被误判"已过期"(红)；影响 getFridgeDetail/getFridgeList/getExpiringItems/getItemsByLayer + 客户端 fridge.ts:64。getDefaultFridge 用 toDateStr 字符串比较是正确的，两种范式并存。建议抽统一 isExpired/isWarning 工具。
  - **P1-01**：item-edit.ts:111 `zoneNames.indexOf(item.zoneId)`（zoneNames 是名称数组、zoneId 是 ID）永远 -1，编辑保存后物品静默搬到首分区首层。
  - **P1-02**：mine.ts:80-86 加载失败注入伪造冰箱 demo_001 并置 fridgesReady=true。
- 待确认(高)：generateQRCode scene=`fridgeId|role|Date.now()`≈47字节，超微信 32 字节上限，真机分享码可能整体失败。
- 注：第一轮修复的 commit 5311d23 仅本地提交，push 因 github.com:443 连接超时失败（网络问题），待重推；新云函数 generateQRCode/updateUserNotify/createFridge 需重新上传部署。
