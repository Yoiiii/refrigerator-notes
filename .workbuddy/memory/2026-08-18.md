
## 模板 ID 与头像昵称显示（2026-08-18）
- 用户补充订阅消息模板：ID `6x2llq5TwIj-EkeFnpDi2M6rmBNc5-a-wke0wl6bk8E`，字段 `thing1=物品名称 / date3=到期时间 / thing4=存放位置`。
- checkExpiry 云函数已替换真实模板 ID 并按要求映射字段，thing 类字段做 18 字符截断。
- P2-13 简化为只展示：mine 页点击用户卡片区调用 `wx.getUserProfile` 获取微信头像昵称并同步到 users 表；member-manage 列表使用成员 `avatarUrl` 显示头像。
- 提交 `43ee5f9`（本地，未推送，按用户约定只 commit 不 push）。需重新部署云函数 `checkExpiry`、`login`。
