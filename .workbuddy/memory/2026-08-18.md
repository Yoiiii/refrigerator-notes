
## 模板 ID 与头像昵称显示（2026-08-18）
- 用户补充订阅消息模板：ID `6x2llq5TwIj-EkeFnpDi2M6rmBNc5-a-wke0wl6bk8E`，字段 `thing1=物品名称 / date3=到期时间 / thing4=存放位置`。
- checkExpiry 云函数已替换真实模板 ID 并按要求映射字段，thing 类字段做 18 字符截断。
- P2-13 简化为只展示：mine 页点击用户卡片区调用 `wx.getUserProfile` 获取微信头像昵称并同步到 users 表；member-manage 列表使用成员 `avatarUrl` 显示头像。
- 提交 `43ee5f9`（本地，未推送，按用户约定只 commit 不 push）。需重新部署云函数 `checkExpiry`、`login`。

## 左滑删除失效修复 + 详情骨架屏（2026-08-18 午）
- 左滑删除失效根因：`swipe-cell.ts` 的 `onTouchMove` 位移上限 `Math.min(Math.max(offset, -self.data.rightWidth),0)` 误用 `self.data.rightWidth`（P2-07 改为实例属性 `_rightWidth` 后恒为 0），导致左滑负位移被钳回 0。改为 `-self._rightWidth`；并在 `onTouchStart` 预调用 measure 避免首滑吃帧。
- item-detail 新增骨架屏：`loading` 状态控制，加载期间展示头部缩略图+信息列表 shimmer 动画。
- 提交 `7249c66`（本地，未推送）。前端改动编译即可见，无需重新部署云函数。

## 订阅授权接入 + 定时调整（2026-08-18 下午）
- item-edit `onSave` 在用户点击手势内（首个 await 前）同步调用 `wx.requestSubscribeMessage({ tmplIds: [EXPIRY_TEMPLATE_ID] })` 申请临期提醒授权；用户关闭通知（globalData.userInfo.notifyEnabled===false）时不打扰。既补全了 checkExpiry 缺失的授权前置条件。
- checkExpiry 定时器触发器由 `0 0 1 * * * *`（01:00）改为 `0 0 9 * * * *`（09:00），避免凌晨推送打扰。
- EXPIRY_TEMPLATE_ID 常量定义在 item-edit.ts 顶部，与云函数保持一致。
- 提交 `5b8771d`（本地，未推送）。**需重新上传部署 cloud 函数 `checkExpiry`**（config.json 的 timer 触发器随部署生效）。

## 全量回归测试（miniapp-tester 程小测，2026-08-18 晚）
- 静态走查全 10 页 + utils + 2 组件 + 20 云函数；输出缺陷报告 `.workbuddy/regression-defect-report-2026-08-18.md`。
- 结论：无 P0；P1×2、P2×10。
  - P1-01：`fridge.ts` `loadFridgeData` catch 无错误态/空态（权限失效时显示空壳）。
  - P1-02：冰箱页只读成员可见并能点击无效删除按钮（WXML 三处 `slot="right"` 无条件渲染，且 `role` 未存入 data 致无法做权限分支；`onDeleteConfirm` catch 为空）。
  - P2 重点：call() 无 timeout；`getItemsByLayer`/`getDefaultFridge` 死代码；`generateQRCode` scene 恰 32 字符临界；写入类云函数未捕获 checkFridgePermission 抛错；删除默认冰箱后 defaultFridgeId 悬空等。
- 已核对：鉴权链（`checkFridgePermission` + 物品归属校验 + manageMember owner 守卫/事务）完整可靠；前后端数据契约一致；shared/auth.js 已按函数各自内置副本（require 路径 `./shared/auth` 正确）。

## 隐私授权弹窗接入（2026-08-18 晚，WeChatMiniProgramDeveloper）
- app.json 增加 `"__usePrivacyCheck__": true`；app.ts `onLaunch` 调 `initPrivacy()`：首次启动用 `wx.getPrivacySetting` 查 `needAuthorization`，为 true 时 `wx.requirePrivacyAuthorize` 主动弹微信原生授权框（拒绝则 toast 提示「部分功能需授权后使用」）。
- 开启后 `getUserProfile` / `chooseMedia` / `saveImageToPhotosAlbum` 三个隐私接口在用户未授权时由微信自动弹原生框，无需改页面。
- 用 `const wxp: any = wx` 兜底，避免旧版 miniprogram-api-typings 未声明隐私接口而 tsc 报类型错。
- 注意：弹窗文案取 MP 后台「用户隐私保护指引」，需先在后台发布指引，否则 `requirePrivacyAuthorize` 不会弹出。

## 缺陷修复落地（2026-08-18 晚，接上轮回归报告）
- 已按报告把 P1-01/P1-02/P2-1~P2-7 全部修复并提交 `14f2a6f`（本地，未推送）：
  - P1-01：`fridge.ts` catch 加 `loadError` + toast，WXML 增加 `wx:elif="{{loadError}}"` 空态与「重新加载」。
  - P1-02：`fridge.ts` 把 `role` 存入 data；WXML 删除/添加按钮均加 `wx:if="{{role !== 'readonly'}}"`（含三层布局三处删除按钮 + 三处添加按钮，统一收口只读不可写）。
  - P2-1：`utils/cloud.ts` 的 `call()` 加 15s 超时（Promise + timer 包装，silent 时不重复 toast）。
  - P2-2：删除死代码云函数 `getItemsByLayer`、`getDefaultFridge`（已 grep 确认前端 0 调用）；同步清理 CLAUDE.md 表格引用。
  - P2-3：`addItem`/`updateItem`/`deleteItem`/`deleteFridge` 用 try/catch 包裹 `checkFridgePermission`，抛错返回结构化 `{code,msg}`（err.code||-1），前端拿到明确提示而非裸「操作失败」。
  - P2-4：`deleteFridge` 删除前清理 `users.defaultFridgeId`（捕获 checkFridgePermission 返回的 openid）；`fridge-settings.ts` 删除成功后同步清空 `globalData.userInfo.defaultFridgeId`。
  - P2-5：`item-edit` 保质期 picker 由写死 `2026-01-01~2030-12-31` 改为动态 `今天~今天+5年`（minDate/maxDate）。
  - P2-6：`mine` 页用 `button open-type="chooseAvatar"` + `input type="nickname"` 替换已废弃 `wx.getUserProfile`，分别同步云端 `login`。
  - P2-7：`item-edit.onSave` 订阅授权加 `expirySubscribeAsked` 去重（同设备仅弹一次），避免每次保存都弹。
- 注意：`deleteFridge` 等 4 个云函数改动需重新上传部署；`mine` 头像昵称需基础库支持 chooseAvatar（旧版回退灰色头像+「微信用户」属预期）。

## 缺陷报告更新 + PR 说明（2026-08-18 晚收尾）
- 回归报告 `regression-defect-report-2026-08-18.md` 追加「六、修复状态跟踪」：已修复 9 项（P1-01/P1-02 + P2-01/03/04/06/07/08/13），待修复 6 项（P2-02/05/09/10/11/12）；并修正原报告 P2 计数误差（写明 13 项）。
- 新增独立 PR 说明 `fix-pr-notes-2026-08-18.md`：改动总览表 + 逐项说明 + 部署/验证注意。
- 验证：回读 fridge.ts/cloud.ts/deleteFridge/item-edit.ts/mine.ts/addItem 确认 9 项修复真实落地（信任但验证）。
