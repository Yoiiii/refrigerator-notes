# 2026-08-19 工作日志

- 设计文档 PRD 升级到 **V1.5.0**（`docs/prd-v.1.5.md`，由 v.1.4 复制并同步）：闭环回归报告中 12 项缺陷修复到设计文档，含版本头、修订说明、附录修订记录。
  - 同步要点：冰箱页加载失败错误态+只读成员按 role 隐藏删除/添加按钮；`utils/cloud.ts` call() 15s 超时；写入类云函数（addItem/updateItem/deleteItem/deleteFridge）捕获 checkFridgePermission 异常返回结构化 {code,msg}；删除默认冰箱清理 users.defaultFridgeId；分享码 scene 改为定长无分隔符编码(fridgeId24+roleCode2+dayTs)+长度护栏，扫码端向后兼容旧 `|` 格式；接入微信隐私授权弹窗(__usePrivacyCheck__+首次启动 requirePrivacyAuthorize)；临期0天→「今天到期」；冰箱设置页加载失败错误态；删除/保存二维码失败细分提示；订阅授权同设备去重；预设图标 wx:key 改 *this；移除死代码云函数 getItemsByLayer（云函数清单/权限速查/目录结构/计数同步清理）。
  - 旧格式 `fridgeId|role|timestamp`、getUserProfile、getDefaultFridge 等 stale 引用已从 v1.5 清除（仅保留向后兼容说明中的旧 `fridgeId|role|dayTs`）。
- 按项目约定 **仅 commit 不 push**。
